# 仓库文件清单（Netlify 玩测 / PostHog 回收）

盘点日期：2026-09-20。范围：`chickenstock-yixuan` 当前工作区。

口径：这次目标是把网页发到 Netlify，事件进 PostHog。下面三类按这个目标分，**不是建议立刻删仓库**。

| 类 | 含义 | 这次上线 |
| --- | --- | --- |
| 工程 | 游戏能跑、能导出、能上报所依赖 | 必须保留；其中几份还没进 git |
| 过时 | 曾是权威，现已被别的文件取代 | 不要照着实现；先别当垃圾删 |
| 垃圾 / 草稿 | 运行时和导出都不引用 | 上线可完全忽略；体积大的以后再清 |

`.import` 与对应 PNG/WAV 成对存在，清单里只写源文件。`.godot/` 是本机编辑器缓存，已在 `.gitignore`，不算仓库工程。

---

## 0. 这次上线最小闭环（先看这一段）

按顺序真正会碰到的文件：

1. `project.godot` — 改 `config/version`（这次还是 `2026.09.19-pt1`，发布前必须改，否则新旧数据混在一起）
2. `export_presets.cfg` — Web 单线程、head 里引入 `posthog-config.js` + `analytics.js`
3. Godot 导出到 `export/web/index.html`（生成 `.wasm` / `.pck`，不进 git）
4. `node tools/web-sync.mjs` — 把 `web/analytics.js` 拷进导出目录并体检
5. 整包 `export/web/` 拖到 Netlify 站点 chickenstock
6. 手机打开 https://chickenstock.netlify.app 强制刷新

**钥匙**：`export/web/posthog-config.js` 本机已填美国区 token，被 `.gitignore` 排除，**不要提交**。仓库里的 `web/posthog-config.js` 是空模板，同步脚本不会覆盖已经填好的导出副本。

---

## 1. 工程文件

### 1.1 运行时（会进网页包）

主入口：`scenes/Game.tscn` + `scripts/Game.gd`。

| 路径 | 做什么 |
| --- | --- |
| `project.godot` | 视口 576×1024、主场景、Autoload `Analytics`、触摸模拟鼠标 |
| `export_presets.cfg` | Web 导出：单线程、排除草稿、注入统计脚本 |
| `scenes/Game.tscn` | 农场、HUD、底栏、信封、设置、教学、结算卡节点树 |
| `scenes/SettlementCard.tscn` | 日结 / 通关 / 翻车卡界面 |
| `scenes/SheetSprite.tscn` | 图集动画组件（孵蛋、狼） |
| `scripts/Game.gd` | 整局：经济、模式、结算、重试、教学、存档、点击层级 |
| `scripts/SettlementCard.gd` | 结算卡填数和 CTA |
| `scripts/SurveyCard.gd` | 画布里的六题问卷 |
| `scripts/Analytics.gd` | 事件、有效时长、动作计数；Web 才真正发出去 |
| `scripts/Loc.gd` | 中英文字典和语言防抖 |
| `scripts/Sfx.gd` | 音效 / 鸡叫 / 环境音 |
| `scripts/YardBird.gd` | 院子里鸡走停、挡建筑变淡 |
| `scripts/SheetSprite.gd` | 切图集循环播放 |
| `scripts/DayClock.gd` | 顶栏时钟 |
| `scripts/StockGraph.gd` | 股价折线 |
| `scripts/UiLayout.gd` | 只摆坐标 |
| `scripts/UiStyle.gd` | 按钮按下/悬停 |
| `scripts/Juice.gd` | 飞金币、抖动 |
| `scripts/EditorAssetPlacement.gd` | 仅编辑器可见，运行时隐藏；绑在主场景上，别拆 |
| `materials/bird_ghost.tres` `materials/bird_outline.tres` | 鸡挡住建筑时的材质 |
| `shaders/bird_ghost.gdshader` `shaders/sprite_outline.gdshader` | 上两项的着色器 |
| `themes/mobile.tres` `themes/frame_tall_panel.tres` `themes/frame_ticker_panel.tres` | 主题和弹窗框 |
| `fonts/game_zh_full.otf` | 项目正在用的中英字体 |
| `icon.png` | 应用图标 |
| `web/analytics.js` | 第一方 PostHog 客户端（COEP 下不能引官方 SDK） |
| `web/posthog-config.js` | token 模板（仓库里 apiKey 为空） |
| `netlify.toml` | Git 连接 Netlify 时的发布目录和 COOP/COEP |
| `export/web/netlify.toml` `export/web/_headers` | 手动拖拽部署时必须和 `index.html` 同级；导出会冲掉，导完检查还在 |

**地图 / 角色（主场景或 YardBird 实际 load）**

- `assets/map/farm-background.png`
- `assets/sprites/hatch-8.png`
- `assets/sprites/hen.png` `assets/sprites/chick-character.png`
- `assets/sprites/hen-idle-8.png` `assets/sprites/hen-walk-8.png`
- `assets/sprites/chick-idle-8.png` `assets/sprites/chick-walk-8.png`
- `assets/sprites/wolf-hen-idle-6x1.png`

**主界面 UI**

- `assets/ui/menu-background.png` `assets/ui/logo_icon.png` `assets/ui/logo_wordmark.png`
- `assets/ui/clock-face.png` `assets/ui/clock-hour-hand.png`
- `assets/ui/mail.png` `assets/ui/close.png` `assets/ui/egg-basket.png`
- `assets/ui/guide-board-cut.png` `assets/ui/guide-chick.png` `assets/ui/guide-stock.png`
- `assets/ui/farm-ui/settings_gear.png` `assets/ui/farm-ui/speech.png`
- `assets/ui/farm-ui/hud_bar.png` `assets/ui/farm-ui/chip_value.png` `assets/ui/farm-ui/icon_coin.png`
- `assets/ui/farm-ui/simple-ui/` 整目录（panel / 三色按钮 / tag / chip / flourish）
- `assets/ui/weather-sun.png` `weather-cloud.png` `weather-rain.png` `weather-moon.png`

**结算卡 kit + 正在用的 layers**

- `assets/ui/settlement/kit/` 下正在被 `SettlementCard.gd` / `UiLayout.gd` 读的编号图（01、04–07、09–11、21–24、28–30、32–33、35）
- `assets/ui/settlement/layers/00-moon.png` `01-paper-stack.png` `02-card-body.png` `03-hang-tag.png` `20-score-cell.png` `21-chart-well.png` `22-quest-stamp.png` `23-pip-empty.png` `24-pip-hit.png` `25-pip-now.png`

**小图标 `icons/`（Juice / 奖杯 / 买卖按钮）**

`egg.png` `cake.png` `coin.png` `chick.png` `hen.png` `stock.png` `share.png` `check.png` `alert.png` `rotten_egg.png` `hatch.png` `flock.png` `price_up.png` `price_down.png` `settings.png` `trophy.png`

**音效**

`assets/sfx/chick_1.wav` `chick_2.wav` `chick_3.wav` `hen_1.wav` `hen_2.wav`  
`assets/sfx/CREDITS.txt`（授权说明，建议留）

### 1.2 开发工程（不进网页包，仓库要留）

冒烟测试（导出预设已排除 `tests` 场景名以外的 py/md/tools，但这些场景本身不要当垃圾）：

| 路径 | 验什么 |
| --- | --- |
| `tests/TutorialSmokeTest.tscn` + `TutorialSmokeRunner.gd` | 教学能走完 |
| `tests/LayerSmokeTest.tscn` + `LayerSmokeRunner.gd` | 信封/结算/底栏层级 |
| `tests/GateRetrySmokeTest.tscn` + `GateRetrySmokeRunner.gd` | 第 12/16/20 天各一次当天重试 |
| `tests/EndlessSmokeTest.tscn` + `EndlessSmokeRunner.gd` | 无尽解锁与续日 |
| `tests/SurveySmokeTest.tscn` + `SurveySmokeRunner.gd` | 问卷提交后入口改成 5 秒等待 |

发布脚本：

| 路径 | 做什么 | git 状态 |
| --- | --- | --- |
| `tools/web-sync.mjs` | 导出后同步统计文件并体检 | **未跟踪，必须留下并纳入仓库** |
| `tools/web-serve.mjs` | 本机带 COOP/COEP 预览导出包 | **未跟踪，建议纳入** |
| `tools/sync_web_shell.py` | 同上，Python 版 | 已跟踪；本机 python 是商店占位，先用 mjs |
| `tools/serve_web.py` | 同上，Python 版 | 已跟踪 |

权威文档（改代码前读这些，不要读过时那批）：

| 路径 | 地位 |
| --- | --- |
| `AGENTS.md` | 层级、双击、烤炉、Deploy SOP |
| `CONTEXT.md` | 玩测术语（当天重试 ≠ 重新开局） |
| `docs/ANALYTICS.md` | **事件字典唯一口径** |
| `docs/PLAYTEST_DECISIONS.md` | 问卷/复活规则的用户决策 |
| `docs/adr/0001-first-party-analytics-client-under-coep.md` | 为什么自己写 analytics.js |
| `docs/LAUNCH_CHECKLIST.md` | 上线阻塞项（**未跟踪**） |
| `docs/今天怎么上线.md` | 人按步骤导出/上传（**未跟踪**） |
| `.gitignore` `.gitattributes` `.editorconfig` | 仓库约定 |
| `.cursor/rules/farm-*.mdc` | Agent 规则，对应 AGENTS 各段 |

对版场景（不进导出包，太阳海改画还要用）：

- `scenes/ArtScene.tscn` `scripts/ArtScene.gd`
- `scenes/AssetGallery.tscn` `scripts/AssetGallery.gd`
- `scenes/SettlementPreview.tscn` `scripts/SettlementPreview.gd`

切图/打包工具（改美术时用，导出已排除 `tools/*`）：

`tools/_pack_dock_ui.py` `_pack_hatch.py` `_pack_logo.py` `_slice_kit_sheet.py` `_trim_voices.py` `recut_chick_sheets.py` `recut_hen_sheets.py` `write_game_tscn.py` `sim_full_journey.py` `sim_challenge_casual.py`  
`fonts/_subset_zh.py` `fonts/_check_glyphs.py`  
`assets/ui/settlement/_pack_layers.py` `_extract_card.py`

### 1.3 本机部署产物（工程，但不要进 git）

| 路径 | 说明 |
| --- | --- |
| `export/web/index.html` `.wasm` `.pck` 以及 Godot 生成的 js | 导出后才有；现在还没有，所以还上不了线 |
| `export/web/posthog-config.js` | 已填 token，gitignore 正确 |
| `export/web/analytics.js` | 跑 `web-sync.mjs` 之后才会出现 |

---

## 2. 过时文件

这些不是空目录垃圾：曾经指导过实现，正文里仍有过期判断。上线不要照抄；冲突时以 §1.2 的权威文档为准。

| 路径 | 过时点 | 现在看哪份 |
| --- | --- | --- |
| `docs/PLAYTEST_ANALYTICS_PLAN.md` | 写过「工程里没有挑战模式」，基线判断已失效 | `docs/ANALYTICS.md` + `PLAYTEST_DECISIONS.md` |
| `docs/CURSOR_HANDOFF_ANALYTICS_SURVEY_PLATFORMS.md` | 09-17 交接快照：当时埋点/问卷都还没做 | 同上；过程记录可留 |
| `docs/ANALYTICS_HANDOFF.md` | 自己写「ANALYTICS.md 已过时」——这句话反了，事件字典已经落地 | `docs/ANALYTICS.md` |
| `docs/ENDLESS_MODE_PLAN.md` | 行号停在 09-05 的 Game.gd；无尽已接好，计划里「待做」部分过期 | 以当前 `Game.gd` 为准 |
| `docs/UI_LAYOUT_WORKFLOW.md` | 排版流程仍可用，但是旧一轮 UI 优化手册，不是上线步骤 | 改 UI 才读 |
| `docs/PLATFORM_PLAYTEST_RESEARCH.md` | CrazyGames/Poki 调研；本次是自有站 Netlify，不是平台提交 | 以后投平台再读 |
| `监工文档.md` | 08-30 / 09-06 验收记录；仍写「三日新手关」，实际是单日 8 步 | `AGENTS.md` + 当前教学代码 |
| `tmp/analytics-plan-update/PLAYTEST_ANALYTICS_PLAN.md` | 正式文档的临时副本 | 删或移走都可以，不要再改它 |
| `tmp/analytics-plan-update/PLAYTEST_ANALYTICS_PLAN.original.md` | 同上 | 同上 |
| `fonts/game_zh.ttf` | 项目主题用的是 `game_zh_full.otf`，这份子集当前无引用 | 确认无 fallback 后再标垃圾 |

`tools/sync_web_shell.py` / `serve_web.py` **没有过时**，只是本机暂时跑不了 Python；功能由 `tools/web-sync.mjs` / `web-serve.mjs` 承担。

---

## 3. 垃圾 / 草稿（运行时 0 引用，导出已排除）

上线完全用不到。体积大约：`generated-v2` 38 MB，结算 mockup 5 MB，高清底图 3 MB。不要在这次发布前 rewrite git 历史去删。

### 3.1 空场景、参考代码、临时目录

| 路径 | 为什么算垃圾 |
| --- | --- |
| `node_2d.tscn` | 只有一个空 Node2D，导出已排除 |
| `icons_preview.tscn` | 图标墙，不进主场景 |
| `ref/layout.tsx` `ref/page.tsx` `ref/globals.css` `ref/sfx.ts` `ref/atlas.csv` | 早期网页原型，Godot 工程不读 |
| `tmp/analytics-plan-update/` | 见上一节，副本 |

### 3.2 生图草稿（`AGENTS.md` 写明不给 Game 用）

整个目录：`assets/ui/farm-ui/generated-v2/`（含 `kit-sheet-v3/`、各张 2–3 MB 的 HUD/对话框草稿）。有 `.gdignore`，编辑器也不该扫它。

### 3.3 对版 mockup / 弃用图集 / 旧底图

| 路径 | 说明 |
| --- | --- |
| `assets/ui/settlement/mockups/daily.png` `finale.png` | 只给 SettlementPreview 看 |
| `assets/map/farm-background-hd-v2.png` | 只给 SettlementPreview；主游戏用 `farm-background.png` |
| `assets/ui/dialogue-boxes/` | 对话气泡草稿，主场景不用 |
| `assets/ui/cozyland-source/` | ArtScene 还写着路径，目录已经不存在 |
| `assets/ui/farm-ui-atlas-transparent.png` `farm-ui-atlas-v1.png` `ui-atlas-parchment.png` `game-icons-spritesheet-v2.png` | 旧图集，运行时已改用单件 PNG |
| `assets/ui/settlement-card-transparent.png` | 旧整卡，现已分层 |
| `icons/atlas-8x4.png` | 旧图标图集 |

### 3.4 旧角色条 / 静帧（已被 8 帧 idle/walk 或主场景新图替换）

| 路径 | 现在用的是 |
| --- | --- |
| `assets/sprites/chick-bake-8frames.png` | 未接入 |
| `assets/sprites/chick-walk-8frames.png` | `chick-walk-8.png` |
| `assets/sprites/farm-sheet-8x5.png` | SheetSprite 默认值，主游戏不播这条 |
| `assets/sprites/chick.png` | `chick-character.png` / idle-walk 条 |
| `assets/sprites/egg-sack-static.png` | 未接入 |
| `assets/sprites/merchant-group-shadow.png` | 未接入 |
| `assets/sprites/ground-sketch-static.png` | 未接入 |
| `assets/sprites/wolf-merchant.png` | 主场景用 `wolf-hen-idle-6x1.png` |
| `assets/map/incubator.png` | 孵化器在底图里，没有单独节点 |

`tools/_hen_src/hen-idle-8.png` `hen-walk-8.png` 是切图源，不是运行时。

### 3.5 旧 UI 单图（主界面已换成 simple-ui / kit）

主游戏和结算卡都不再 `load` 这些（ArtScene / 监工文档当参考的除外）：

`assets/ui/btn-green.png` `btn-red.png` `btn-play.png`  
`assets/ui/build-bakery.png` `build-barn.png` `build-house.png` `build-incubator.png` `buy-chick.png` `sell-hen.png`  
`assets/ui/chip.png` `clock.png` `cozy-clock.png` `day-track.png` `dialog.png`  
`assets/ui/guide-eggs.png` `guide-eggs-cut.png` `guide-one-page.png`  
`assets/ui/heart-bar.png` `hud-bar.png` `ico-home.png` `ico-shop.png` `journal.png`  
`assets/ui/logo.png` `logo-cn.svg` `logo-en.svg` `logo-icon.svg`  
`assets/ui/map-pin.png` `panel.png` `parchment.png` `quest-board.png` `ribbon.png`  
`assets/ui/settings.png` `settings-btn-beige.png` `settings-btn-green.png` `settings-btn-red.png` `settings-btn-segment.png`  
`assets/ui/signboard.png` `speech.png` `star-bar.png` `wide-panel.png`  
`assets/ui/upgrade-oven-button.svg` `weather-partly.png`  
`assets/ui/farm-ui/button_green.png` `button_red.png` `button_neutral.png` `button_empty_*.png`  
`assets/ui/farm-ui/clock_bezel.png` `day_track.png` `thought_bubble.png` `frame_tall.png`  
`assets/ui/farm-ui/icon_cake.png` `icon_chick.png` `icon_egg.png`  
`assets/ui/farm-ui/dock_*.png`（底栏现用代码样式 + simple-ui）

结算 layers 里导出已排除、主结算卡也不再当背景铺的：

`assets/ui/settlement/layers/10-news-chip.png` `11-price-well.png` `12-ledger-cell.png` `13-footnote-strip.png`

`icons/` 里主游戏没用到的：`breeze.png` `buy_chick.png` `clock.png` `cloud.png` `coins.png` `crown.png` `egg_basket.png` `gem.png` `heart.png` `moon.png` `rain.png` `ribbon.png` `sell_hen.png` `sfx.png` `sun.png` `wolf.png`（奖杯/天气若以后用再挪，现在可当库存）

---

## 4. 导出排除名单和主场景打架（上线风险，不是垃圾）

`export_presets.cfg` 的 `exclude_filter` 把下面这些标成「不要打进包」，但 **`Game.tscn` / `Game.gd` 还在引用**：

| 被排除的模式 | 实际还在用 |
| --- | --- |
| `*logo_wordmark*` | 主菜单标题图 |
| `*logo_icon.png` | 主菜单图标 + 无尽奖杯 |
| `*hud_bar.png` | 顶栏 HUD 条 |

如果这次导出后主菜单缺 logo、顶栏缺木条，先把这三项从 `exclude_filter` 拿掉，**不要删源文件**。

另：`*ArtScene*` `*AssetGallery*` `*SettlementPreview*` `*icons_preview*` `node_2d.tscn` `*generated-v2*` `*settlement/mockups*` 排除是对的。

---

## 5. 这次明确不要动

- 不要提交 `export/web/posthog-config.js`
- 不要清空 `export_presets.cfg` 的排除项（除了上一节那三个可能误伤）
- 不要为了「仓库干净」在发布前批量删 `generated-v2` 并 rewrite 历史
- 不要改 `Loc.gd` 文案、音效手感、字号贴边（那是太阳海的）

未跟踪、上线前建议加进 git 的工程文件：

- `tools/web-sync.mjs`
- `tools/web-serve.mjs`
- `tests/SurveySmokeRunner.gd.uid`
- `docs/LAUNCH_CHECKLIST.md`
- `docs/今天怎么上线.md`
- 本清单 `docs/仓库文件清单.md`
