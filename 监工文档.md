# UI 监工文档

## 新手指南可编辑 UI（2026-08-30）

### 素材抠图验收

| 素材 | 结果 | 说明 |
| --- | --- | --- |
| `guide-board-cut.png` | 通过 | 已清除四角浅色棋盘格，画布外角 Alpha 为 0；木板与羊皮纸仍保持完整。 |
| `guide-eggs.png` | 不采用 | 原图四角仍为不透明的浅色棋盘格，不适合游戏内叠放。 |
| `egg-basket.png` | 通过 | 改用项目已有透明蛋篮素材，四角 Alpha 为 0。 |
| `guide-chick.png` | 通过 | 四角 Alpha 为 0，透明边缘正常。 |
| `guide-stock.png` | 通过 | 四角 Alpha 为 0，透明边缘正常。 |

### 字号统一验收（576×1024 手机基准）

- 指南主标题：25 px，与主要弹窗标题的 24–28 px 范围一致。
- 步骤标题：22 px，与 HUD 数字和关键操作文字的 20–22 px 范围一致。
- 副标题与步骤说明：14 px，与现有说明文字的 13–16 px 范围一致。
- 结论：通过；正文在手机上保持可读，层级清晰。

### 布局验收

- 指南面板使用 10%–90% 横向安全区、3.5%–96.5% 纵向安全区，避开手机边缘。
- 内容区置于羊皮纸内侧 11%–89%、15%–89%，避免压到木夹和木框。
- 三个步骤固定为纵向卡片，每张最低 135 px；文字区优先扩展，插画保持 128×128 的等比显示。
- 关闭按钮始终位于最上层右上角；点击卡片外区域也会关闭指南。
- 结论：通过；后续可在 `GuidePop/GuideCard/Content` 单独拖动每一张卡、每一个文本和插画。

### 回归验证

- Godot 无界面加载：通过。
- `TutorialSmokeTest.tscn`：`TUTORIAL_SMOKE_OK`。
- 已知非阻塞警告：`res://Game.tscn` 与 `res://scenes/Game.tscn` 的 UID 重复，尚未处理。

## 设置页真实 UI 置换（2026-08-30）

- 设置页仍由 Godot 的 `Button`、`Label` 与 `PanelContainer` 组成；未将示意图当作整张可点击图片贴入游戏。
- “中文”与 “English” 已拆为两个独立按钮，当前语言以绿色高亮，点击非当前语言才会切换。
- “新手指南”入口更名为“新手关卡”，仍打开现有新手指南内容；“重玩三日新手关”仍启动教学流程。
- 帮助信息栏已从设置页隐藏；重置游戏改为独立红色危险操作。
- 卡片纵向安全区扩大至 6%–90%，单行最小触控高度调整为 62–64 px。
- 回归：`TutorialSmokeTest.tscn` 通过（`TUTORIAL_SMOKE_OK`）。

### 设置按钮资产验收

- 已新增透明按钮皮肤：`settings-btn-beige.png`、`settings-btn-green.png`、`settings-btn-red.png`、`settings-btn-segment.png`。
- 四枚资产的画布角落 Alpha 均为 0；绿色与红色资产已清除生成时遗留的浅色棋盘格。
- 使用 `StyleBoxTexture` 的纹理边距实现九宫格拉伸，横向扩展时保留圆角、木边和边缘细节。
- 文字与点击事件仍属于真实 Godot `Button`，不会烘焙进图片。
- 修正记录：首次使用时发现素材画布的透明留白导致按钮视觉被压成细线；已裁切至有效像素边界，并将九宫格纹理边距调整为横向 70、纵向 20。
- 复核：生成按钮还包含不适合九宫格的横向尖角装饰，运行时会形成异常长边。已停止将其作为按钮皮肤；设置页恢复项目原生按钮样式，功能和布局正常。透明按钮素材保留为设计参考，不参与运行时绘制。

## 简洁通用 UI 替换（2026-09-06）

- 新增并验收五个透明、裁切后的通用皮肤：`panel`、`button-beige`、`button-green`、`button-red`、`tag`。
- 定稿路径：`assets/ui/farm-ui/simple-ui/`；生成草图路径：`assets/ui/farm-ui/generated-v2/simple-ui/`。
- 所有定稿均为 RGBA，四角 Alpha 为 0；中心区域保持平整、角落对称，适合 Godot 九宫格拉伸。
- 设置页面板切换为简洁羊皮纸框；常规、主操作、危险操作及语言未选中态分别使用米色、绿色、红色与标签皮肤。
- 原功能、文字、触控大小、语言防抖、结算层级未改动。
- 回归：Godot 资源导入正常；`TutorialSmokeTest.tscn` 输出 `TUTORIAL_SMOKE_OK`。
- 边缘修正：原九宫格切线落在圆角曲线中，造成按钮横向拉伸后出现尖头。运行时皮肤现统一为 256 px 宽，按钮横向切线 48 px、纵向切线 16 px；面板横向切线 52 px、纵向切线 20 px，确保只拉伸中间平整区域。

## 字体大小设置（2026-09-06）

- 设置页新增“小 / 标准 / 大”三档字体选择，当前档位用绿色按钮高亮。
- 字体缩放值为 85%、100%、115%，会应用到已有标签与按钮文字；按钮触控面积保持不变。
- 选择会存入 `user://cluck-settings.json`，下次打开自动恢复。
- 回归：`TutorialSmokeTest.tscn` 输出 `TUTORIAL_SMOKE_OK`。
